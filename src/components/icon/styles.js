import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    images: {
        width: '100%',
        height: '100%'
    },
    icon: {
        width: 25,
        height: 25
    }
})

export default styles;