import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    card: {
        borderRadius: 8,
        width: '100%',
        overflow: 'hidden',
    },
    images: {
        width: '100%',
        height: '100%'
    }
})

export default styles